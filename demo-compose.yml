version: "3.3"

services:
  nginx:
    image: nginx:stable-alpine
    volumes:
      - ./docs:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.demo.rule=Host(`demo.wuhanstudio.uk`)"
        - "traefik.http.routers.demo.entrypoints=web"
        - "traefik.http.services.demo.loadbalancer.server.port=80"
        # TLS
        - "traefik.http.routers.demos.rule=Host(`demo.wuhanstudio.uk`)"
        - "traefik.http.routers.demos.entrypoints=websecure"
        - "traefik.http.routers.demos.tls.certresolver=myhttpchallenge"
        # Redirect
        - "traefik.http.routers.demo.middlewares=https_redirect"
        - "traefik.http.middlewares.https_redirect.redirectscheme.scheme=https"
    networks:
      - traefik-public
networks:
  traefik-public:
    external: true
